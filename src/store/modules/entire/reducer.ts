const initializeState = {
  currentPage: 0,
};
function reducer(state = initializeState, action: { type: string }) {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;
